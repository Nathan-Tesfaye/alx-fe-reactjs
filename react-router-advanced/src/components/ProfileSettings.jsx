import React from "react";

const ProfileSettings = () => {
  return (
    <div>
      <h2>Profile Settings</h2>
      <p>This is where profile settings can be configured.</p>
    </div>
  );
};

export default ProfileSettings;
